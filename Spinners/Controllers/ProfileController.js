angular
  .module("spinnerApp")
  .controller("ProfileController", function ($scope, $rootScope, $state, $stateParams, menuService, customerService, cartService, homeService, NgMap, userService) {
  })
